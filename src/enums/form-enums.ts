export enum ControlType {
  number = 'number',
  text = 'text',
  'text-area' = 'text-area',
  email = 'email',
  select = 'select',
  checkbox = 'checkbox',
  radioGroup = 'radioGroup',
  date = 'date',
  'datetime-local' = 'datetime-local',
}
